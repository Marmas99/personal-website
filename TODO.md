# TODO — Customize Your Resumé Website

- [ ] **Edit `src/data/resume.ts`** — Replace the placeholder content with your real info (name, jobs, skills, education, social links, email)
- [ ] **Set `base` in `vite.config.ts`** — Change `'/personal-website/'` to match your actual GitHub repo name
- [ ] **Update `homepage` in `package.json`** — Match your GitHub Pages URL
- [ ] **Optional: Add a PDF** — Place a `resume.pdf` in `/public` and uncomment `resumeUrl` in the data file
